<div class="page-top">
  <h1>Costumes</h1>

  <div class="costumes-card-container product-card">
    <% @costumes.each do |costume| %>
      <%= render 'shared/card', costume: costume%>
    <% end %>
  </div>
</div>

<%= simple_form_for [@costume, @booking] do |f| %>
  <%= f.input :date_start %>
  <%= f.input :date_end %>
  <%= f.association :costume %>
  <%= f.association :user %>
  <%= f.submit 'book now', :onclick => "return confirm('booking confirmed')" %>
<% end %>
